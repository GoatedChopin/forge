// `#[forge::forge_enum]` already derives Debug/Clone/Copy/PartialEq/Eq/Hash/Serialize/Deserialize
// and implements sqlx::Type / Encode / Decode + Display/FromStr. Don't add those derives manually.

#[allow(unused_imports)]
use forge::prelude::*;

#[forge::forge_enum]
pub enum {{Name}} {
    Pending,
    Active,
    Archived,
}
