[project]
name = "kanban-board"

[database]
url = "${DATABASE_URL}"

[gateway]
port = 8080
cors_origins = ["http://localhost:5173"]

[auth]
jwt_algorithm = "HS256"
jwt_secret = "${JWT_SECRET}"

[observability]
enabled = true
otlp_endpoint = "http://otel:4318"
